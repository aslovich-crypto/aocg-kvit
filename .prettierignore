# Вендорные копии дизайн-системы. Файлы приходят из проекта Claude Design
# машинной выгрузкой (tools/build-tokens.js) и сверяются побайтно контрольной
# суммой — форматирование сломало бы сверку и превратило бы «копия совпадает
# с источником» в «копия совпадает с нашим prettier».
design/

dist/

# Вендорная копия шрифта Inter (T43). Файлы и CSS перенесены ДОСЛОВНО из
# ответа Google Fonts, чтобы отрисовка не изменилась и чтобы обновление
# сводилось к перекачиванию того же запроса. Форматирование превратило бы
# «копия совпадает с источником» в «копия совпадает с нашим prettier».
public/fonts/
scripts/probe-summa/__dist/
